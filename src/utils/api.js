import {
    _getUsers,
    _getQuestions,
    _saveQuestion,
    _saveQuestionAnswer,
} from './_DATA.js'

export function search ({description, full_time, location}) {
    return _saveQuestion(info)
}